import { Typography, Container } from '@mui/material';

const Weather = () => {
  return (
    <Container>
      <Typography variant="h3" gutterBottom>
        Weather Page
      </Typography>
      <Typography variant="body1">
        this page was created simply as an example of how to place the page
        panel on the site.
      </Typography>
    </Container>
  );
};

export default Weather;
